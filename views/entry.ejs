<div class="row single-entry">
  <article class="active">
    <div class="contain">
      <header>
      <% var entry = 0 %>
        <h1><%= data[entry].title %></h1>
      </header>
      <section>
        <% if (data[entry].media) { %>
        <%- include media-handler %>
        <% } %>
        <%- data[entry].content %>
      </section>
      <footer>
        <cite><a href="/users/entries/<%= data[entry].postedBy %>"><%= data[entry].postedBy %></a> &rarr; <a href="/sub/<%= data[entry].postedTo %>"><%= data[entry].postedTo %></a></cite>
      </footer>
    </div>
  </article>
</div>
<div class="row single-entry">
    <% for(i = 0; i< data[entry].comments.length; i++) {%>
        <article class="active">
            <div class="contain">
                <header>
                    <%=data[entry].comments[i].postedBy %>
                </header>
                <section>
                    <%=data[entry].comments[i].content %>
                </section>
            </div>
        </article>
    <% } %>
</div>
